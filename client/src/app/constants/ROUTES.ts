export enum ROUTES {
  ENTITIES = 'entities',
  ENTITY = 'entity',
  COLLECTIONS = 'collections',
  COLLECTION = 'collection',
  IDENTIFIER = 'id',
  ERROR = 'error',
  NOT_FOUND = 'not-found',
}

export enum PARAMS {
  UUID = 'uuid',
}

export enum REASONS {
  CONFIG = 'config',
  SERVER = 'server',
}
