export enum ROUTES {
  ENTITIES = 'entities',
  COLLECTIONS = 'collections',
  IDENTIFIER = 'id',
  ERROR = 'error',
  NOT_FOUND = 'not-found',
}

export enum PARAMS {
  UUID = 'uuid',
}

export enum REASONS {
  CONFIG = 'config',
  SERVER = 'server',
}
