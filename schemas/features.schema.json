{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ramen-schema.org/ora/2026.1/features.schema.json",
  "title": "ORA Features",
  "description": "Configuration for optional feature-specific helpers used by the ORA application.",
  "$ref": "#/$defs/features",
  "$defs": {
    "bibleBook": {
      "type": "object",
      "description": "Configuration entry for one biblical book, including its canonical key and accepted aliases.",
      "additionalProperties": false,
      "required": ["key", "aliases"],
      "properties": {
        "key": {
          "type": "string",
          "description": "Canonical book key used internally."
        },
        "aliases": {
          "type": "array",
          "description": "Alternative names, abbreviations, or spellings that should resolve to this biblical book.",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "features": {
      "type": "object",
      "description": "Feature configuration grouped by feature area.",
      "additionalProperties": false,
      "required": ["bible"],
      "properties": {
        "bible": {
          "type": "array",
          "description": "Configured biblical books used for Bible reference detection and normalization.",
          "items": {
            "$ref": "#/$defs/bibleBook"
          }
        }
      }
    }
  }
}
